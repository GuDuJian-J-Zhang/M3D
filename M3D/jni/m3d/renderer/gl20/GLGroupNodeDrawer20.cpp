#include "GLGroupNodeDrawer20.h"

#include "m3d/scene/GroupNode.h"
namespace M3D
{
//void GLGroupNodeDrawer20::DrawGroupNode(SceneNode* node, RenderAction* action)
//{
//	GroupNode* groupNode = (GroupNode*) node;
//	RenderContext* gl = action->GetGLContext();
//	gl->PushMatrix();
//	LoadMatrix(groupNode, gl);
//	for (int i = 0; i < groupNode->Size(); i++)
//	{
//		groupNode->GetChild(i)->Draw(action);
//	}
//	gl->PopMatrix();
//}

}

